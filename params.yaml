# Hiperparámetros del pipeline reproducible (DVC-native). El motor los ancla en `dvc.lock`.
# `seed` FIJO (determinismo del entrenamiento, Art.15). El arco rojo→verde lo gobierna
# `mitigate`: demo-repos.ts lo bumpea false→true como TRATAMIENTO versionado en git (ISO 23894 §6.5).
seed: 42
mitigate: false
